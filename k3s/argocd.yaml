# ArgoCD reads this file (bootstrap: argocd/appsets/) and builds this app's
# Application from it. This directory is the app; ArgoCD applies every other
# manifest in it and skips this one.
name: epg
namespace: epg

# autoSync: false leaves the app on manual sync in the UI.
autoSync: true
prune: true
selfHeal: true
