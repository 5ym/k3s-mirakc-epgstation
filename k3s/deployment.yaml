apiVersion: apps/v1
kind: Deployment
metadata:
  name: mirakurun
  namespace: epg
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mirakurun
  template:
    metadata:
      labels:
        app: mirakurun
    spec:
      containers:
        - name: mirakurun
          image: ghcr.io/danything/mirakurun:sha-f5dbf332bcba
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          env:
            - name: TZ
              value: Asia/Tokyo
          ports:
            - containerPort: 40772
          volumeMounts:
            - name: mirakurun-data
              mountPath: /app-data
            # 設定。スキャン結果が書き戻されるのでPVCに置く
            - name: mirakurun-config
              mountPath: /app-config
            - name: dev-bus
              mountPath: /dev/bus
            - name: dev-dvb
              mountPath: /dev/dvb
            # pcscd がここに socket を作る。denpa からも同じものを見せて、
            # スクランブルの解除だけカードを借りられるようにする
            - name: pcscd
              mountPath: /run/pcscd
      volumes:
        - name: mirakurun-data
          persistentVolumeClaim:
            claimName: mirakurun-data
        - name: mirakurun-config
          persistentVolumeClaim:
            claimName: mirakurun-config
        - name: dev-bus
          hostPath:
            path: /dev/bus
            type: Directory
        - name: dev-dvb
          hostPath:
            path: /dev/dvb
            type: Directory
        - name: pcscd
          hostPath:
            path: /run/denpa-pcscd
            type: DirectoryOrCreate
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: denpa
  namespace: epg
spec:
  replicas: 1
  # 録画とエンコードを抱えるので、新旧2Podが同時に走らないようにする
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: denpa
  template:
    metadata:
      labels:
        app: denpa
    spec:
      containers:
        - name: denpa
          image: ghcr.io/danything/denpa:sha-80b0c6c8cd1e
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: Asia/Tokyo
            # SvelteKit の CSRF 判定は Origin ヘッダと自分の origin を突き合わせる。
            # adapter-node は ORIGIN も PROTOCOL_HEADER も無いと https と決め打つので、
            # 平文で開いている dp.home.arpa からの POST が全部 403 になっていた
            # (画面上は「押しても何も起きない」に見える)。
            # ORIGIN は1つしか書けず dp.doany.io と両立しないので、
            # Traefik が付ける x-forwarded-proto をリクエストごとに見る。
            # HOST_HEADER は既定の host のままにする。Traefik は元の Host を渡すし、
            # kubelet のヘルスチェックは x-forwarded-host を付けないため
            - name: PROTOCOL_HEADER
              value: x-forwarded-proto
            - name: MIRAKURUN_URL
              value: http://mirakurun:40772
            - name: DENPA_DB
              value: /app/data/denpa.db
            - name: RECORDED_DIR
              value: /app/recorded
            - name: LIBRARY_DIR
              value: /library
            # CMは既定ではチャプターを打つだけ。実カットは番組ごとにUIで選ぶ
            - name: CM_CUT_DEFAULT
              value: chapter
            - name: CM_DETECTOR
              value: silence
            # 録画のコーデック。非力なノードでは h264 にする
            - name: ENCODE_CODEC
              value: av1
            - name: ENCODE_CONCURRENCY
              value: '2'
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 30
          volumeMounts:
            - name: denpa-data
              mountPath: /app/data
            - name: denpa-recorded
              mountPath: /app/recorded
            - name: library
              mountPath: /library
            # Mirakurun 側の pcscd の socket。カードを直接は掴まない
            - name: pcscd
              mountPath: /run/pcscd
      volumes:
        - name: denpa-data
          persistentVolumeClaim:
            claimName: denpa-data
        - name: denpa-recorded
          persistentVolumeClaim:
            claimName: denpa-recorded
        - name: library
          persistentVolumeClaim:
            claimName: denpa-library
        - name: pcscd
          hostPath:
            path: /run/denpa-pcscd
            type: DirectoryOrCreate
