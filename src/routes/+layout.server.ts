/**
 * ログインしている人。**OIDC を通って入ったときだけ入る。**
 *
 * 素通し (LAN) で入っている人は `null` のまま。ヘッダーには何も出さない。
 *
 * ここで出すのは名前だけ。誰であるかは `sub` が決めていて、そちらは画面に要らない
 * (出しても読めない文字列で、コピーされて困るものでもないが、使い道が無い)。
 */
export function load({ locals }) {
    return { user: locals.user ?? null };
}
