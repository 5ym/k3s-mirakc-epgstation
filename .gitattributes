# PowerShell 5.1 は BOM が無いと .ps1 を ANSI (日本語環境では CP932) として読む。
# UTF-8 の日本語が化けるだけでなく、CP932 の先行バイトが次の ASCII 文字を巻き込むため
# ' や } が消えて構文エラーになる。BOM はファイルの中身なのでそのまま残るが、
# 改行は PS 5.1 に合わせて CRLF で置く
*.ps1 text eol=crlf
